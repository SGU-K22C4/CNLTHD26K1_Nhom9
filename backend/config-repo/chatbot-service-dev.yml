spring:
  data:
    mongodb:
      uri: ${SPRING_DATA_MONGODB_URI:mongodb+srv://monimal1:huytran123@monimal1.meiqplv.mongodb.net/fashion_chatbot_db?retryWrites=true&w=majority&appName=monimal1}

chatbot:
  security:
    allow-direct-user-header: true
  resilience:
    timeout-ms: 3500
    retry:
      max-attempts: 2

ai:
  log-requests: true
  log-responses: true
